# 🧠 Neuromodulation LLM - Minimal Requirements
# Install with: pip install -r requirements-minimal.txt
# This covers only the essential dependencies for basic functionality

# =============================================================================
# CORE MACHINE LEARNING (Required)
# =============================================================================
# PyTorch with CUDA 11.8 support (for GPU acceleration)
# This adds PyTorch's CUDA wheel index as an extra source
# If you need CPU-only PyTorch, install separately: pip install torch torchvision torchaudio
--extra-index-url https://download.pytorch.org/whl/cu118
torch>=2.2.0                    # PyTorch for deep learning (CUDA-enabled)
torchvision>=0.17.0             # Torchvision (CUDA-enabled)
torchaudio>=2.2.0               # Torchaudio (CUDA-enabled)
transformers>=4.42.0            # Hugging Face transformers
accelerate>=0.20.0              # Hugging Face accelerate
hf-transfer>=0.1.0              # Faster Hugging Face downloads

# =============================================================================
# SCIENTIFIC COMPUTING (Required)
# =============================================================================
numpy>=1.25.0                   # Numerical computing
scipy>=1.11.0                   # Scientific computing

# =============================================================================
# BASIC UTILITIES (Required)
# =============================================================================
requests>=2.31.0                # HTTP requests
tqdm>=4.65.0                    # Progress bars
